/**
 * Event strings object
 */
define({
    /* The browser window has been resized */
    windowResizeEvent: 'windowResized',
    /* The state of the page has changed */
    stateChangeEvent: 'stateChanged',
    /* The Projects div has shown or hidden its scrollbar */
    scrollChangeEvent: 'scrollBoxChanged',
    /* The Projects div has been scrolled */
    scrollEvent: 'scrolled',
    /* The current focused layer has changed */
    focusChangeEvent: 'focusChanged'
});